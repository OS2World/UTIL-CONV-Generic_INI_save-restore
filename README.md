UTIL-CONV-Generic_INI_save-restore
==================================

A program (DumpINI) to convert an OS/2 binary INI file into a human-readable file; and a program (LoadINI) to do the reverse translation.
